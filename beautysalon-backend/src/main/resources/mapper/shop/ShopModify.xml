<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.salon.beauty.model.mapper.shop.ShopModifyMapper">
    <update id="shopModify" parameterType="ShopModifyRequestDTO">
        UPDATE SHOP
        SET SHOP_LOGIN_ID=#{shop_login_id}
            ,SHOP_PASSWORD=#{shop_password}
            ,SHOP_NAME=#{shop_name}
            ,SHOP_PHONE=#{shop_phone}
            ,SHOP_CEO=#{shop_ceo}
            ,SHOP_CEO_PHONE=#{shop_ceo_phone}
            ,POINT_ACCUMULATION_RATE=#{point_accumulation_rate}
            ,POINT_ACCUMULATION_CONDITION=#{point_accumulation_condition}
        WHERE SHOP_ID=#{shop_id}
    </update>
</mapper>